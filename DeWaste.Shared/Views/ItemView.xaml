<Page
    x:Class="DeWaste.Views.ItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DeWaste.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Black">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="3*" />
        </Grid.RowDefinitions>
        <Image x:Name ="itemImage" Source="{x:Bind ViewModel.ItemImage, Mode=OneWay}" Stretch="Uniform" Grid.Row="0"></Image>
        <StackPanel Grid.Row="1">
            <TextBlock x:Name="itemName" FontSize="40" HorizontalAlignment="Center" Margin="0, 0, 0, 20" Text ="{x:Bind ViewModel.ItemName, Mode=OneWay}"/>
            <TextBlock x:Name="itemDesc" FontSize="20" HorizontalAlignment="Center" Margin="0, 0, 0, 20" 
                       TextWrapping="Wrap" Text="{x:Bind ViewModel.ItemDescription, Mode=OneWay}"/>
        </StackPanel>
    </Grid>
</Page>
